// GENERATED by scripts/embed-viewer.mjs — do not edit by hand.
// Fallback static assets for the compiled binary (no viewer/ on disk).

export const viewerAssets: Record<string, string> = {
  "doc.js": "// Document reading page (C): rendered md + cite highlight with 3-tier fallback.\n//\n// Tier 1 (default cite target): rendered view, TreeWalker match on a\n// markdown-stripped projection of the quote. Tier 2: source view — quote is a\n// verbatim substring of the md source (validated at import), indexOf always\n// hits. Tier 3: visible notice, never a silent failure.\n\nimport { api, rawDoc, esc, entityHref, docHref, methodBadge, truncate } from '/util.js';\n\nconst MATH_TOKEN = 'KGMATH';\n\nconst renderMarkdown = (md) => {\n  const maths = [];\n  const tokenized = md.replace(/\\$\\$[\\s\\S]+?\\$\\$|\\$[^$\\n]+\\$/g, (m) => {\n    maths.push(m);\n    return `${MATH_TOKEN}${maths.length - 1}END`;\n  });\n  let html = marked.parse(tokenized);\n  html = html.replace(new RegExp(`${MATH_TOKEN}(\\\\d+)END`, 'g'), (_, i) => {\n    const m = maths[+i];\n    const display = m.startsWith('$$');\n    const tex = m.replace(/^\\$+|\\$+$/g, '');\n    try { return katex.renderToString(tex, { displayMode: display, throwOnError: false }); }\n    catch { return esc(m); }\n  });\n  return html;\n};\n\n// The chrome <h1> already shows the title; drop a duplicating leading md H1\n// from the RENDERED view only (source view stays verbatim for cite tier 2).\nconst stripDupTitle = (md, title) => {\n  const m = /^\\s*#\\s+(.+?)\\s*\\n/.exec(md);\n  return m && m[1].trim() === title.trim() ? md.slice(m[0].length) : md;\n};\n\n// Strip md markers so the quote matches rendered textContent.\nconst projectQuote = (q) => q\n  .replace(/```[a-z]*\\n?/g, '').replace(/^#+\\s*/gm, '')\n  .replace(/\\*\\*([^*]+)\\*\\*/g, '$1').replace(/\\*([^*]+)\\*/g, '$1')\n  .replace(/`([^`]+)`/g, '$1').replace(/\\[([^\\]]+)\\]\\([^)]*\\)/g, '$1')\n  .replace(/\\s+/g, ' ').trim();\n\nconst highlightRendered = (container, quote) => {\n  const target = projectQuote(quote);\n  if (!target) return false;\n  const walker = document.createTreeWalker(container, NodeFilter.SHOW_TEXT);\n  let text = '';\n  const nodes = [];\n  for (let n = walker.nextNode(); n; n = walker.nextNode()) {\n    nodes.push({ node: n, start: text.length });\n    text += n.textContent;\n  }\n  const flat = text.replace(/\\s+/g, ' ');\n  // map collapsed index back: search on collapsed text, locate via running scan\n  const idx = flat.indexOf(target);\n  if (idx < 0) return false;\n  // walk原始 text 找到与 collapsed 索引对应的真实区间\n  let ci = 0; let startReal = -1; let endReal = -1;\n  for (let i = 0; i < text.length; i++) {\n    const isWs = /\\s/.test(text[i]);\n    if (isWs && ci > 0 && /\\s/.test(flat[ci - 1] || '')) continue; // collapsed run\n    if (ci === idx && startReal < 0) startReal = i;\n    if (ci === idx + target.length) { endReal = i; break; }\n    ci += 1;\n  }\n  if (startReal < 0) return false;\n  if (endReal < 0) endReal = text.length;\n  // wrap per text node — one range across element boundaries (e.g. into a\n  // <code>) makes surroundContents throw\n  let first = null;\n  for (const { node, start } of nodes) {\n    const s = Math.max(startReal - start, 0);\n    const e = Math.min(endReal - start, node.textContent.length);\n    if (s >= e) continue;\n    const range = document.createRange();\n    range.setStart(node, s);\n    range.setEnd(node, e);\n    const mark = document.createElement('mark');\n    try { range.surroundContents(mark); } catch { continue; }\n    first ??= mark;\n  }\n  if (first) first.scrollIntoView({ block: 'center' });\n  return Boolean(first);\n};\n\nconst highlightSource = (pre, source, quote) => {\n  const idx = source.indexOf(quote);\n  if (idx < 0) return false;\n  pre.innerHTML = `${esc(source.slice(0, idx))}<mark>${esc(quote)}</mark>${esc(source.slice(idx + quote.length))}`;\n  pre.querySelector('mark').scrollIntoView({ block: 'center' });\n  return true;\n};\n\nconst extractPanel = (meta) => {\n  if (!meta) return '<p class=\"muted\">（尚未抽取）</p>';\n  const m = (meta.mentions || []).map((x) => `\n    <li><a href=\"${entityHref(x.concept)}\">${esc(x.concept)}</a>\n      ${x.anchor?.quote ? `<button class=\"cite jump\" data-q=\"${esc(x.anchor.quote)}\">↗</button>` : ''}</li>`);\n  const r = (meta.relations || []).map((x) => `\n    <li>${esc(x.from)} ─${esc(x.relation)}→ ${esc(x.to)} ${methodBadge(x.method, x.confidence)}\n      ${x.anchor?.quote ? `<button class=\"cite jump\" data-q=\"${esc(x.anchor.quote)}\">↗</button>` : ''}</li>`);\n  const l = (meta.doc_links || []).map((x) =>\n    `<li><a href=\"${docHref(x.to_hash)}\">${esc(truncate(x.raw || x.to_hash, 40))}</a></li>`);\n  return `\n    ${meta.summary ? `<p class=\"summary\">${esc(meta.summary)}</p>` : ''}\n    <h3>提及（${m.length}）</h3><ul class=\"plain\">${m.join('') || '<li class=\"muted\">无</li>'}</ul>\n    <h3>关系（${r.length}）</h3><ul class=\"plain\">${r.join('') || '<li class=\"muted\">无</li>'}</ul>\n    ${l.length ? `<h3>文档链接（${l.length}）</h3><ul class=\"plain\">${l.join('')}</ul>` : ''}`;\n};\n\nexport async function renderDoc(el, hash, cite) {\n  const [info, source] = await Promise.all([api('doc', { hash }), rawDoc(hash)]);\n  el.innerHTML = `\n    <div class=\"doc-layout\">\n      <aside class=\"doc-side\"><h2>本文抽取</h2>${extractPanel(info.metadata)}</aside>\n      <article class=\"doc-main\">\n        <div class=\"doc-head\">\n          <h1>${esc(info.title)}</h1>\n          <div class=\"doc-actions\">\n            <span class=\"muted\">${esc(info.path)}</span>\n            <button id=\"view-toggle\">源码视图</button>\n            <a class=\"btn\" href=\"${esc(info.vscode_url)}\">在编辑器打开 ↗</a>\n          </div>\n          <div id=\"cite-notice\" class=\"hidden\"></div>\n        </div>\n        <div id=\"doc-rendered\" class=\"md-body\"></div>\n        <pre id=\"doc-source\" class=\"hidden\"></pre>\n      </article>\n    </div>`;\n  const rendered = el.querySelector('#doc-rendered');\n  const pre = el.querySelector('#doc-source');\n  const notice = el.querySelector('#cite-notice');\n  rendered.innerHTML = renderMarkdown(stripDupTitle(source, info.title));\n  pre.textContent = source;\n  const toggle = el.querySelector('#view-toggle');\n  const setMode = (src) => {\n    rendered.classList.toggle('hidden', src);\n    pre.classList.toggle('hidden', !src);\n    toggle.textContent = src ? '渲染视图' : '源码视图';\n  };\n  toggle.addEventListener('click', () => setMode(pre.classList.contains('hidden')));\n\n  const jumpTo = (quote) => {\n    notice.classList.add('hidden');\n    setMode(false);\n    rendered.querySelectorAll('mark').forEach((m) => m.replaceWith(...m.childNodes));\n    if (highlightRendered(rendered, quote)) return;       // tier 1\n    setMode(true);\n    if (highlightSource(pre, source, quote)) return;      // tier 2\n    notice.textContent = '⚠ 无法精确定位该引文（文档可能已变更），已显示全文。'; // tier 3\n    notice.classList.remove('hidden');\n  };\n  el.querySelectorAll('.jump').forEach((b) =>\n    b.addEventListener('click', () => jumpTo(b.dataset.q)));\n  if (cite) jumpTo(cite);\n}\n",
  "entity.js": "// Entity hub page (B): header + out/in edges with provenance + mentioning docs.\n\nimport { api, esc, typeBadge, edgeRow, entityHref, docHref, truncate } from '/util.js';\n\nexport async function renderEntity(el, name) {\n  const data = await api('entity', { name });\n  if (!data) { el.innerHTML = `<p class=\"error\">没有这个实体：${esc(name)}</p>`; return; }\n  const e = data.entity;\n  // doc→entity \"mentions\" edges are listed as 提及文档, the rest as relation edges\n  const isMention = (r) => r.relation === 'mentions' && r.from?.kind === 'doc';\n  const mentions = data.in_edges.filter(isMention);\n  const inEdges = data.in_edges.filter((r) => !isMention(r));\n  el.innerHTML = `\n    <div class=\"entity-head\">\n      <h1>${esc(e.name)} ${typeBadge(e.type)}\n        <span class=\"muted\">被 ${e.mention_count} 篇提及</span></h1>\n      ${e.aliases?.length ? `<p class=\"muted\">别名：${e.aliases.map(esc).join(' · ')}</p>` : ''}\n      ${e.summary ? `<p class=\"summary\">${esc(e.summary)}</p>` : ''}\n      <p><a class=\"btn\" href=\"#/graph?focus=${encodeURIComponent(e.name)}\">🕸 在图谱中查看</a></p>\n    </div>\n    <div class=\"cols\">\n      <section><h2>出边（${e.name} →）</h2>\n        <ul class=\"plain\">${data.out_edges.map((r) => edgeRow(r, 'to')).join('')\n          || '<li class=\"muted\">无</li>'}</ul></section>\n      <section><h2>入边（→ ${esc(e.name)}）</h2>\n        <ul class=\"plain\">${inEdges.map((r) => edgeRow(r, 'from')).join('')\n          || '<li class=\"muted\">无</li>'}</ul></section>\n    </div>\n    <section><h2>被这些文档提及（${mentions.length}）</h2>\n      <ul class=\"plain doclist${mentions.length > 6 ? ' cols-list' : ''}\">${mentions.map((r) => `\n        <li><a href=\"${docHref(r.from.hash, r.anchor)}\">${esc(r.from.title)}</a>\n          ${r.anchor ? `<span class=\"muted\">“${esc(truncate(r.anchor, 60))}”</span>` : ''}\n          <a class=\"cite\" href=\"${docHref(r.from.hash, r.anchor)}\">↗</a></li>`).join('')\n        || '<li class=\"muted\">无</li>'}</ul></section>`;\n}\n",
  "graph.js": "// Graph view (D): Focus (ego) / Overview (skeleton) over a cached full export.\n\nimport {\n  api, esc, fullGraph, TYPE_COLORS, typeBadge, methodBadge, entityHref, docHref,\n} from '/util.js';\n\nconst SIZE = (w) => Math.min(56, Math.max(14, 14 + 6 * Math.sqrt(w || 1)));\n\nconst STYLE = [\n  { selector: 'node[kind=\"entity\"]', style: {\n    'background-color': (n) => TYPE_COLORS[n.data('type')] || '#888',\n    width: (n) => SIZE(n.data('weight')), height: (n) => SIZE(n.data('weight')),\n    label: 'data(label)', 'font-size': 10, color: '#2A2A28',\n    'text-valign': 'bottom', 'text-margin-y': 4, 'min-zoomed-font-size': 8 } },\n  { selector: 'node[kind=\"doc\"]', style: {\n    shape: 'round-rectangle', 'background-color': '#E8E8E4', 'border-width': 1,\n    'border-color': '#999', width: 16, height: 12, label: 'data(label)',\n    'font-size': 8, color: '#777', 'text-valign': 'bottom', 'min-zoomed-font-size': 10 } },\n  { selector: 'node.focus', style: { 'border-width': 4, 'border-color': '#E8A33D' } },\n  { selector: 'edge', style: {\n    'curve-style': 'bezier', 'target-arrow-shape': 'triangle', 'arrow-scale': 0.7,\n    'line-color': '#8AAFD0', 'target-arrow-color': '#8AAFD0', 'line-style': 'dashed',\n    'line-dash-pattern': [6, 3],\n    width: (e) => Math.min(3.5, 1 + 2.5 * (e.data('confidence') || 0)),\n    opacity: (e) => Math.min(1, 0.25 + 0.75 * (e.data('confidence') || 0)) } },\n  { selector: 'edge[method=\"deterministic\"]', style: {\n    'line-style': 'solid', 'line-color': '#5B5B57', 'target-arrow-color': '#5B5B57',\n    width: 2, opacity: 1 } },\n  { selector: 'edge.hl', style: { 'line-color': '#E8A33D', 'target-arrow-color': '#E8A33D', opacity: 1 } },\n];\n\nconst FILTERS_HTML = `\n  <h3>类型</h3>\n  ${Object.keys(TYPE_COLORS).map((t) => `<label>\n    <input type=\"checkbox\" class=\"f-type\" value=\"${t}\" ${t === 'doc' ? '' : 'checked'}>\n    <i style=\"background:${TYPE_COLORS[t]}\"></i>${t}</label>`).join('')}\n  <h3>method</h3>\n  <label><input type=\"checkbox\" class=\"f-method\" value=\"deterministic\" checked>确定</label>\n  <label><input type=\"checkbox\" class=\"f-method\" value=\"llm\" checked>抽取</label>\n  <h3>confidence ≥ <span id=\"f-conf-v\">0.0</span></h3>\n  <input type=\"range\" id=\"f-conf\" min=\"0\" max=\"1\" step=\"0.05\" value=\"0\">\n  <h3>weight ≥ <span id=\"f-weight-v\">1</span></h3>\n  <input type=\"range\" id=\"f-weight\" min=\"1\" max=\"10\" step=\"1\" value=\"1\">`;\n\nconst readFilters = (el) => ({\n  types: new Set([...el.querySelectorAll('.f-type:checked')].map((x) => x.value)),\n  methods: new Set([...el.querySelectorAll('.f-method:checked')].map((x) => x.value)),\n  minConf: +el.querySelector('#f-conf').value,\n  minWeight: +el.querySelector('#f-weight').value,\n});\n\nfunction buildElements(g, mode, focus, f) {\n  const nodeOk = (n) => f.types.has(n.kind === 'doc' ? 'doc' : n.type)\n    && (n.kind === 'doc' || (n.weight || 0) >= f.minWeight);\n  const edgeOk = (e) => f.methods.has(e.method)\n    && (e.method === 'deterministic' || e.confidence >= f.minConf);\n  let nodes; let edges;\n  if (mode === 'focus' && focus) {\n    const fnode = g.nodes.find((n) => n.kind === 'entity' && n.label === focus);\n    if (!fnode) return { nodes: [], edges: [] };\n    const near = new Set([fnode.id]);\n    g.edges.forEach((e) => {\n      if (e.source === fnode.id) near.add(e.target);\n      if (e.target === fnode.id) near.add(e.source);\n    });\n    nodes = g.nodes.filter((n) => near.has(n.id) && (n.id === fnode.id || nodeOk({ ...n, weight: 99 })));\n    const ids = new Set(nodes.map((n) => n.id));\n    edges = g.edges.filter((e) => ids.has(e.source) && ids.has(e.target) && edgeOk(e));\n  } else {\n    // overview skeleton: entity-entity, high-trust, hot nodes\n    nodes = g.nodes.filter((n) => n.kind === 'entity' && nodeOk(n));\n    const ids = new Set(nodes.map((n) => n.id));\n    edges = g.edges.filter((e) => ids.has(e.source) && ids.has(e.target) && edgeOk(e)\n      && (e.method === 'deterministic' || e.confidence >= Math.max(f.minConf, 0.7)));\n    const connected = new Set(edges.flatMap((e) => [e.source, e.target]));\n    nodes = nodes.filter((n) => connected.has(n.id));\n  }\n  return {\n    nodes: nodes.map((n) => ({ data: { ...n, ...(focus && n.label === focus ? { isFocus: 1 } : {}) },\n                               classes: focus && n.label === focus ? 'focus' : '' })),\n    edges: edges.map((e) => ({ data: { ...e, id: `${e.source}|${e.relation}|${e.target}` } })),\n  };\n}\n\nconst sideEntity = async (side, name) => {\n  const d = await api('entity', { name });\n  if (!d) return;\n  side.innerHTML = `\n    <h3><a href=\"${entityHref(name)}\">${esc(name)}</a> ${typeBadge(d.entity.type)}</h3>\n    <p class=\"muted\">提及 ${d.entity.mention_count} · ${esc(d.entity.summary || '')}</p>\n    <ul class=\"plain small\">${[...d.out_edges.map((r) => `<li>─${esc(r.relation)}→ ${esc(r.to.name || r.to.title)}</li>`),\n      ...d.in_edges.map((r) => `<li>${esc(r.from.name || r.from.title)} ─${esc(r.relation)}→</li>`)]\n      .slice(0, 14).join('')}</ul>\n    <p class=\"muted\">双击节点 = 移动焦点</p>`;\n};\n\nconst sideEdge = async (side, data) => {\n  const details = await api('edge', { source: data.source, target: data.target, relation: data.relation });\n  side.innerHTML = details.map((d) => `\n    <div class=\"edge-card\">\n      <p>${esc(d.from.name || d.from.title)} ─<b>${esc(d.relation)}</b>→\n         ${esc(d.to.name || d.to.title)} ${methodBadge(d.method, d.confidence)}</p>\n      ${d.anchor ? `<blockquote>${esc(d.anchor)}</blockquote>` : ''}\n      ${d.raw ? `<p class=\"muted\">${esc(d.raw)}</p>` : ''}\n      ${d.source ? `<a class=\"btn\" href=\"${docHref(d.source.hash, d.anchor || undefined)}\">打开原文 ↗</a>\n        <span class=\"muted\">${esc(d.source.title)}</span>` : ''}\n    </div>`).join('') || '<p class=\"muted\">无边详情</p>';\n};\n\nexport async function renderGraph(el, focus) {\n  const g = await fullGraph();\n  el.innerHTML = `\n    <div class=\"graph-layout side-empty\">\n      <aside class=\"graph-filters\">${FILTERS_HTML}</aside>\n      <div class=\"graph-main\">\n        <div class=\"graph-bar\">\n          <button id=\"g-focus\" class=\"tab\">Focus</button>\n          <button id=\"g-overview\" class=\"tab\">Overview</button>\n          <input id=\"g-search\" placeholder=\"实体名 → 设为焦点\" list=\"g-names\">\n          <datalist id=\"g-names\">${g.nodes.filter((n) => n.kind === 'entity')\n            .map((n) => `<option value=\"${esc(n.label)}\">`).join('')}</datalist>\n        </div>\n        <div id=\"cy\"></div>\n      </div>\n      <aside class=\"graph-side\" id=\"g-side\"><p class=\"muted\">点节点/边看详情</p></aside>\n    </div>`;\n  let mode = focus ? 'focus' : 'overview';\n  let focusName = focus || null;\n  const side = el.querySelector('#g-side');\n  const layoutEl = el.querySelector('.graph-layout');\n  const openSide = () => {\n    layoutEl.classList.remove('side-empty');\n    cy.resize(); // canvas shrinks when the side panel opens\n  };\n  const cy = cytoscape({\n    container: el.querySelector('#cy'), style: STYLE,\n    wheelSensitivity: 0.3, textureOnViewport: true, hideEdgesOnViewport: true,\n  });\n  window.kgCy = cy; // debug/test handle\n  const redraw = () => {\n    el.querySelector('#g-focus').classList.toggle('active', mode === 'focus');\n    el.querySelector('#g-overview').classList.toggle('active', mode === 'overview');\n    const els = buildElements(g, mode, focusName, readFilters(el));\n    cy.elements().remove();\n    cy.add([...els.nodes, ...els.edges]);\n    const layout = mode === 'focus'\n      ? { name: 'concentric', concentric: (n) => (n.data('isFocus') ? 10 : 1),\n          levelWidth: () => 1, minNodeSpacing: 30, animate: false }\n      : { name: window.cytoscapeFcose ? 'fcose' : 'cose',\n          quality: 'default', animate: false, nodeSeparation: 75, packComponents: true };\n    cy.layout(layout).run();\n  };\n  cy.on('tap', 'node[kind=\"entity\"]', (ev) => {\n    openSide();\n    sideEntity(side, ev.target.data('label'));\n  });\n  cy.on('tap', 'edge', (ev) => {\n    cy.edges().removeClass('hl');\n    ev.target.addClass('hl');\n    openSide();\n    sideEdge(side, ev.target.data());\n  });\n  cy.on('dbltap', 'node[kind=\"entity\"]', (ev) => {\n    focusName = ev.target.data('label');\n    mode = 'focus';\n    redraw();\n  });\n  el.querySelector('#g-focus').addEventListener('click', () => { mode = 'focus'; redraw(); });\n  el.querySelector('#g-overview').addEventListener('click', () => { mode = 'overview'; redraw(); });\n  el.querySelector('#g-search').addEventListener('change', (ev) => {\n    if (ev.target.value) { focusName = ev.target.value; mode = 'focus'; redraw(); }\n  });\n  el.querySelectorAll('.graph-filters input').forEach((i) => {\n    i.addEventListener('input', () => {\n      el.querySelector('#f-conf-v').textContent = (+el.querySelector('#f-conf').value).toFixed(2);\n      el.querySelector('#f-weight-v').textContent = el.querySelector('#f-weight').value;\n      redraw();\n    });\n  });\n  redraw();\n}\n",
  "home.js": "// Home: stats + cross-era bridges (serendipity surface) + area/era axes +\n// hot entities + grouped doc browser. One cached /api/graph drives everything.\n\nimport {\n  api, esc, fullGraph, homeModel, eraOf, ERA_LABELS,\n  typeBadge, entityHref, docHref,\n} from '/util.js';\n\nconst bridgeCards = (bridges) => bridges.slice(0, 24).map(({ node, roam, knowledge }) => `\n  <a class=\"card bridge-card\" href=\"${entityHref(node.label)}\">\n    <div class=\"card-name\">${esc(node.label)}</div>\n    <div>${typeBadge(node.type)}\n      <span class=\"muted\">旧 ${roam} · 今 ${knowledge}</span></div>\n  </a>`).join('');\n\nconst docGroups = (docs, filter) => {\n  const groups = new Map();\n  for (const d of docs) {\n    if (filter.area && d.path.split('/')[0] !== filter.area) continue;\n    if (filter.era && eraOf(d.path) !== filter.era) continue;\n    const key = d.path.split('/')[0];\n    if (!groups.has(key)) groups.set(key, []);\n    groups.get(key).push(d);\n  }\n  return [...groups.entries()].sort((a, b) => b[1].length - a[1].length)\n    .map(([dir, items]) => `\n      <details class=\"doc-group\">\n        <summary>${esc(dir)} <span class=\"muted\">(${items.length})</span></summary>\n        <ul class=\"plain doclist\">${items\n          .sort((a, b) => a.path.localeCompare(b.path))\n          .map((d) => `<li><a href=\"${docHref(d.hash)}\">${esc(d.label)}</a>\n            <span class=\"muted\">${esc(d.path)}</span></li>`).join('')}\n        </ul>\n      </details>`).join('') || '<p class=\"muted\">无匹配文档</p>';\n};\n\nexport async function renderHome(el) {\n  const [stats, graph] = await Promise.all([api('stats'), fullGraph()]);\n  const model = homeModel(graph);\n  const docs = graph.nodes.filter((n) => n.kind === 'doc');\n  const entities = graph.nodes.filter((n) => n.kind === 'entity')\n    .sort((a, b) => (b.weight || 0) - (a.weight || 0));\n  const filter = { area: null, era: null };\n\n  el.innerHTML = `\n    <div class=\"statbar\">\n      ${stats.documents} 文档 · ${stats.entities} 实体 · ${stats.edges} 边\n      <span class=\"muted\">（确定 ${stats.edges_by_method.deterministic || 0} ·\n        抽取 ${stats.edges_by_method.llm || 0}）</span>\n    </div>\n    ${model.bridges.length ? `<section><h2>跨时代桥梁\n        <span class=\"muted small\">2021-23 想过、现在又在研究的概念（${model.bridges.length}）</span></h2>\n      <div class=\"cards\">${bridgeCards(model.bridges)}</div></section>` : ''}\n    <section><h2>热门实体</h2><div class=\"cards\">\n      ${entities.slice(0, 8).map((n) => `\n        <a class=\"card\" href=\"${entityHref(n.label)}\">\n          <div class=\"card-name\">${esc(n.label)}</div>\n          <div>${typeBadge(n.type)} <span class=\"muted\">·${n.weight}</span></div>\n        </a>`).join('')}\n    </div></section>\n    <section><h2>按类型浏览</h2><div class=\"tabs\">\n      ${Object.entries(stats.entities_by_type).map(([t, n]) =>\n        `<button class=\"tab\" data-type=\"${esc(t)}\">${esc(t)} ${n}</button>`).join('')}\n    </div><ul id=\"type-list\" class=\"plain cols-list\"></ul></section>\n    <section><h2>文档（${docs.length}）</h2>\n      <div class=\"chips\" id=\"area-chips\">\n        ${model.areas.map(([a, n]) =>\n          `<button class=\"chip\" data-area=\"${esc(a)}\">${esc(a)} <span class=\"muted\">${n}</span></button>`).join('')}\n      </div>\n      <div class=\"chips\" id=\"era-chips\" style=\"margin-top:8px\">\n        ${Object.entries(model.eras).map(([e2, n]) =>\n          `<button class=\"chip\" data-era=\"${esc(e2)}\">${esc(ERA_LABELS[e2] || e2)} <span class=\"muted\">${n}</span></button>`).join('')}\n      </div>\n      <div class=\"doc-groups\" id=\"doc-groups\"></div>\n    </section>`;\n\n  const groupsEl = el.querySelector('#doc-groups');\n  const redrawDocs = () => { groupsEl.innerHTML = docGroups(docs, filter); };\n  const wireChips = (sel, key) => {\n    el.querySelectorAll(`${sel} .chip`).forEach((b) => b.addEventListener('click', () => {\n      filter[key] = filter[key] === b.dataset[key] ? null : b.dataset[key];\n      el.querySelectorAll(`${sel} .chip`).forEach((x) =>\n        x.classList.toggle('active', x.dataset[key] === filter[key]));\n      redrawDocs();\n    }));\n  };\n  wireChips('#area-chips', 'area');\n  wireChips('#era-chips', 'era');\n  redrawDocs();\n\n  const list = el.querySelector('#type-list');\n  el.querySelectorAll('.tab').forEach((b) => b.addEventListener('click', () => {\n    el.querySelectorAll('.tab').forEach((x) => x.classList.toggle('active', x === b));\n    list.innerHTML = entities.filter((n) => n.type === b.dataset.type).map((n) =>\n      `<li><a href=\"${entityHref(n.label)}\">${esc(n.label)}</a>\n       <span class=\"muted\">·${n.weight}</span></li>`).join('');\n  }));\n}\n",
  "index.html": "<!doctype html>\n<html lang=\"zh-CN\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n<title>kg · 知识图谱</title>\n<link rel=\"stylesheet\" href=\"/style.css\">\n<link rel=\"stylesheet\" href=\"https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css\">\n<script src=\"https://cdn.jsdelivr.net/npm/marked@12.0.2/marked.min.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/cytoscape@3.30.2/dist/cytoscape.min.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/layout-base@2.0.1/layout-base.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/cose-base@2.2.0/cose-base.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/cytoscape-fcose@2.2.0/cytoscape-fcose.js\"></script>\n</head>\n<body>\n<header class=\"topbar\">\n  <a class=\"brand\" href=\"#/\">brain2 · KG</a>\n  <input id=\"global-search\" type=\"search\" placeholder=\"搜实体 / 全文…  (Enter)\">\n  <nav>\n    <a href=\"#/\">首页</a>\n    <a href=\"#/graph\">图谱</a>\n  </nav>\n</header>\n<main id=\"app\"></main>\n<script type=\"module\" src=\"/main.js\"></script>\n</body>\n</html>\n",
  "main.js": "// Hash router. Pages are one-file modules; the table maps each route to its\n// renderer and shell archetype (data = dense full-width, prose = reading\n// column, canvas = fixed-viewport).\n\nimport { errBox, spinner } from '/util.js';\nimport { renderHome } from '/home.js';\nimport { renderSearch } from '/search.js';\nimport { renderEntity } from '/entity.js';\nimport { renderDoc } from '/doc.js';\nimport { renderGraph } from '/graph.js';\n\nconst app = document.getElementById('app');\n\nconst routes = {\n  '': { shell: 'data', go: (parts, p) => renderHome(app, p) },\n  entity: { shell: 'data', go: (parts) => renderEntity(app, decodeURIComponent(parts[1] || '')) },\n  doc: { shell: 'prose', go: (parts, p) => renderDoc(app, parts[1], p.get('cite')) },\n  graph: { shell: 'canvas', go: (parts, p) => renderGraph(app, p.get('focus')) },\n  search: { shell: 'data', go: (parts, p) => renderSearch(app, p.get('q') || '') },\n};\n\nconst setNav = (key) => {\n  document.querySelectorAll('.topbar nav a').forEach((a) => {\n    const target = a.getAttribute('href').slice(2).split('/')[0] || '';\n    if (target === key) a.setAttribute('aria-current', 'page');\n    else a.removeAttribute('aria-current');\n  });\n};\n\nconst route = async () => {\n  const hash = location.hash.slice(1) || '/';\n  const [path, query] = hash.split('?');\n  const params = new URLSearchParams(query || '');\n  const parts = path.split('/').filter(Boolean);\n  const r = routes[parts[0] || ''];\n  if (!r) { app.innerHTML = '<p class=\"error\">未知页面</p>'; return; }\n  app.className = `shell-${r.shell}`;\n  setNav(parts[0] || '');\n  app.innerHTML = spinner;\n  try {\n    await r.go(parts, params);\n  } catch (e) {\n    app.innerHTML = errBox(e);\n  }\n};\n\ndocument.getElementById('global-search').addEventListener('keydown', (e) => {\n  if (e.key === 'Enter' && e.target.value.trim())\n    location.hash = `/search?q=${encodeURIComponent(e.target.value.trim())}`;\n});\n\nwindow.addEventListener('hashchange', route);\nroute();\n",
  "search.js": "// Search results: entity name matches + bm25 full-text hits.\n\nimport { api, esc, typeBadge, entityHref, docHref } from '/util.js';\n\nexport async function renderSearch(el, q) {\n  const [hits, concepts] = await Promise.all([api('search', { q, limit: 30 }), api('concepts')]);\n  const ql = q.toLowerCase();\n  const matched = concepts.filter((c) =>\n    [c.canonical, c.display, ...(c.aliases || [])].some((n) => n.toLowerCase().includes(ql)));\n  el.innerHTML = `\n    <h1>搜索：“${esc(q)}”</h1>\n    ${matched.length ? `<section><h2>实体（${matched.length}）</h2>\n      <div class=\"cards\">${matched.slice(0, 12)\n        .map((c) => `<a class=\"card\" href=\"${entityHref(c.canonical)}\">\n          <div class=\"card-name\">${esc(c.display)}</div><div>${typeBadge(c.type)}</div></a>`)\n        .join('')}</div></section>` : ''}\n    <section><h2>全文 bm25（${hits.length}）</h2><ul class=\"plain doclist cols-list\">\n      ${hits.map((h) => `<li><a href=\"${docHref(h.hash)}\">${esc(h.title)}</a>\n        <span class=\"muted\">${esc(h.path)}</span></li>`).join('') || '<li class=\"muted\">无结果</li>'}\n    </ul></section>`;\n}\n",
  "style.css": "/* Light theme. Tokens first; three shell archetypes (data/prose/canvas). */\n:root {\n  /* surface */\n  --bg: #FAFAF8; --surface: #fff; --ink: #2A2A28; --ink-muted: #8a8a85;\n  --ink-sub: #555; --line: #e4e4df; --line-soft: #ececea; --field: #d8d8d2;\n  /* accent / semantic */\n  --accent: #4C72B0; --link: #3A5887; --danger: #C44E52; --cite: #937860;\n  --mark: #FFE9A8; --notice-bg: #FFF6E0; --notice-line: #E8C97A;\n  --bridge: #55A868;\n  /* spacing (4px base) */\n  --s1: 4px; --s2: 8px; --s3: 12px; --s4: 16px; --s5: 24px; --s6: 32px;\n  /* radii */\n  --r-sm: 6px; --r-md: 8px; --r-pill: 16px; --r-badge: 9px;\n  /* type */\n  --fs-h1: 22px; --fs-h2: 16px; --fs-h3: 13px; --fs-body: 14px; --fs-sm: 12px;\n  /* layout */\n  --topbar-h: 44px; --measure: 72ch; --rail: 150px; --panel: 280px;\n  --doc-side: 260px; --shell-pad: 24px;\n}\n\n* { box-sizing: border-box; }\nbody { margin: 0; background: var(--bg); color: var(--ink);\n  font: var(--fs-body)/1.6 -apple-system, \"PingFang SC\", \"Helvetica Neue\", sans-serif; }\na { color: var(--link); text-decoration: none; }\na:hover { text-decoration: underline; }\nh1 { font-size: var(--fs-h1); } h2 { font-size: var(--fs-h2); margin: 18px 0 8px; }\nh3 { font-size: var(--fs-h3); margin: 12px 0 6px; color: var(--ink-sub); }\n.muted { color: var(--ink-muted); font-size: var(--fs-sm); }\n.small { font-size: var(--fs-sm); }\n.error { color: var(--danger); }\n.hidden { display: none !important; }\n\n.topbar { display: flex; align-items: center; gap: var(--s4); padding: var(--s2) 20px;\n  background: var(--surface); border-bottom: 1px solid var(--line);\n  position: sticky; top: 0; z-index: 10; }\n.brand { font-weight: 700; color: var(--ink); }\n.topbar input { flex: 1; max-width: 420px; padding: 6px 10px;\n  border: 1px solid var(--field); border-radius: var(--r-sm); background: var(--bg); }\n.topbar nav { display: flex; gap: var(--s3); align-items: center; }\n.topbar nav a[aria-current] { color: var(--ink); font-weight: 600; }\n.topbar button { cursor: pointer; border: 1px solid var(--field); background: var(--surface);\n  border-radius: var(--r-sm); padding: 5px 10px; }\n\n/* Unified shell: full-width always; archetype decides the content strategy. */\n#app { padding: var(--s4) var(--shell-pad) 48px; }\n.statbar { padding: 10px 0 4px; font-size: 15px; }\n\n.cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));\n  gap: var(--s3); }\n.card { border: 1px solid var(--line); background: var(--surface);\n  border-radius: var(--r-md); padding: 10px 14px; }\n.card:hover { border-color: var(--accent); text-decoration: none; }\n.card-name { font-weight: 600; margin-bottom: var(--s1); color: var(--ink); }\n\n.badge { display: inline-block; padding: 1px 7px; border-radius: var(--r-badge);\n  font-size: 11px; color: #fff; vertical-align: 1px; }\n.badge.det { background: #5B5B57; }\n.badge.llm { background: var(--surface); color: #A8842C; border: 1px solid #CCB974; }\n\n.tabs, .chips { display: flex; gap: var(--s2); flex-wrap: wrap; }\n.tab, .chip { cursor: pointer; border: 1px solid var(--field); background: var(--surface);\n  border-radius: var(--r-pill); padding: 4px 12px; font-size: 13px; }\n.tab.active, .chip.active { background: var(--accent); color: #fff;\n  border-color: var(--accent); }\n.chip .muted { font-size: 11px; }\n.chip.active .muted { color: #dfe6f2; }\n\nul.plain { list-style: none; padding: 0; margin: 6px 0; }\nul.plain li { padding: 4px 0; border-bottom: 1px dashed var(--line-soft); }\n.doclist li { display: flex; gap: 10px; align-items: baseline; }\n.edge-row .cite, .cite { color: var(--cite); font-size: var(--fs-sm); }\n.cite.jump { background: none; border: none; cursor: pointer; }\n.btn { display: inline-block; border: 1px solid var(--field); background: var(--surface);\n  border-radius: var(--r-sm); padding: 4px 10px; font-size: 13px; cursor: pointer; }\n\n.cols { display: grid; grid-template-columns: 1fr 1fr; gap: var(--s5); }\n.entity-head .summary { font-size: 15px; }\n.cols-list { columns: 2; column-gap: var(--s6); }\n.cols-list li { break-inside: avoid; }\n\n/* home: bridge cards + grouped doc list */\n.bridge-card { border-left: 3px solid var(--bridge); }\n.doc-groups { columns: 2; column-gap: var(--s6); }\n.doc-group { break-inside: avoid; margin-bottom: var(--s2); }\n.doc-group summary { cursor: pointer; font-weight: 600; padding: var(--s1) 0; }\n.doc-group summary .muted { font-weight: 400; }\n\n/* doc reading page (shell-prose): full-width shell, prose capped at --measure */\n.doc-layout { display: grid; grid-template-columns: var(--doc-side) minmax(0, 1fr);\n  gap: var(--s5); }\n.doc-side { font-size: 13px; border-right: 1px solid var(--line-soft);\n  padding-right: 14px; position: sticky; top: calc(var(--topbar-h) + var(--s4));\n  align-self: start; max-height: calc(100vh - var(--topbar-h) - var(--s6));\n  overflow-y: auto; }\n.doc-actions { display: flex; gap: 10px; align-items: center; margin: 6px 0; }\n.doc-actions button { cursor: pointer; border: 1px solid var(--field);\n  background: var(--surface); border-radius: var(--r-sm); padding: 3px 8px;\n  font-size: var(--fs-sm); }\n.md-body { max-width: var(--measure); }\n.md-body pre { background: #f3f3ef; padding: 10px; border-radius: var(--r-sm);\n  overflow-x: auto; }\n.md-body code { background: #f3f3ef; padding: 1px 4px; border-radius: 3px; }\n.md-body img { max-width: 100%; }\n#doc-source { white-space: pre-wrap; background: var(--surface);\n  border: 1px solid var(--line); padding: 14px; border-radius: var(--r-sm);\n  font-size: 13px; max-width: var(--measure); }\nmark { background: var(--mark); padding: 1px 0; }\n#cite-notice { background: var(--notice-bg); border: 1px solid var(--notice-line);\n  padding: 6px 10px; border-radius: var(--r-sm); margin: 8px 0;\n  max-width: var(--measure); }\n\n/* graph page (shell-canvas): fixed-viewport canvas */\n.graph-layout { display: grid; grid-template-columns: var(--rail) 1fr var(--panel);\n  gap: 14px; height: calc(100vh - var(--topbar-h) - 46px); }\n.graph-layout.side-empty { grid-template-columns: var(--rail) 1fr 0; }\n.graph-layout.side-empty .graph-side { padding-left: 0; border: none; overflow: hidden; }\n.graph-filters { font-size: var(--fs-sm); overflow-y: auto; }\n.graph-filters label { display: flex; align-items: center; gap: 5px; padding: 2px 0; }\n.graph-filters i { width: 10px; height: 10px; border-radius: 50%; display: inline-block; }\n.graph-filters input[type=range] { width: 100%; }\n.graph-main { display: flex; flex-direction: column; min-width: 0; }\n.graph-bar { display: flex; gap: var(--s2); margin-bottom: var(--s2); }\n.graph-bar input { flex: 1; padding: 4px 8px; border: 1px solid var(--field);\n  border-radius: var(--r-sm); }\n#cy { flex: 1; background: var(--surface); border: 1px solid var(--line);\n  border-radius: var(--r-md); }\n.graph-side { overflow-y: auto; font-size: 13px; border-left: 1px solid var(--line-soft);\n  padding-left: 12px; }\n.edge-card blockquote { border-left: 3px solid var(--bridge); margin: 8px 0;\n  padding: 4px 10px; background: #f6faf6; }\n",
  "util.js": "// Shared helpers: API fetch, escaping, badges, routing links.\n\nexport const api = async (name, params = {}) => {\n  const qs = new URLSearchParams(\n    Object.entries(params).filter(([, v]) => v !== undefined && v !== null && v !== ''));\n  const res = await fetch(`/api/${name}?${qs}`);\n  if (!res.ok) throw new Error((await res.json().catch(() => ({}))).error || res.statusText);\n  return res.json();\n};\n\nexport const rawDoc = async (hash) => {\n  const res = await fetch(`/raw/${hash}`);\n  if (!res.ok) throw new Error('文档不存在');\n  return res.text();\n};\n\nexport const esc = (s) => String(s ?? '')\n  .replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;')\n  .replaceAll('\"', '&quot;');\n\n// Visual encoding (from the design spec)\nexport const TYPE_COLORS = {\n  concept: '#4C72B0', method: '#55A868', framework: '#C44E52',\n  paper: '#8172B3', person: '#CCB974', reference: '#937860', doc: '#999990',\n};\n\nexport const typeBadge = (type) =>\n  `<span class=\"badge type\" style=\"background:${TYPE_COLORS[type] || '#888'}\">${esc(type)}</span>`;\n\nexport const methodBadge = (method, confidence) => method === 'deterministic'\n  ? '<span class=\"badge det\">确定</span>'\n  : `<span class=\"badge llm\">抽取 ${confidence != null ? Number(confidence).toFixed(2) : ''}</span>`;\n\nexport const entityHref = (name) => `#/entity/${encodeURIComponent(name)}`;\nexport const docHref = (hash, cite) =>\n  `#/doc/${hash}${cite ? `?cite=${encodeURIComponent(cite)}` : ''}`;\n\nexport const truncate = (s, n = 48) => {\n  const t = String(s ?? '').replace(/\\s+/g, ' ').trim();\n  return t.length > n ? `${t.slice(0, n)}…` : t;\n};\n\n// One edge row used by entity hub / QA: relation + peer + badges + quote + ↗\nexport const edgeRow = (edge, peerKey) => {\n  const peer = edge[peerKey];\n  const peerLink = peer.kind === 'entity'\n    ? `<a href=\"${entityHref(peer.name)}\">${esc(peer.name)}</a>`\n    : `<a href=\"${docHref(peer.hash)}\">${esc(peer.title)}</a>`;\n  const arrow = peerKey === 'to' ? `─${esc(edge.relation)}→ ${peerLink}`\n                                 : `${peerLink} ─${esc(edge.relation)}→`;\n  const src = edge.source;\n  const cite = edge.anchor && src\n    ? ` <a class=\"cite\" href=\"${docHref(src.hash, edge.anchor)}\"\n         title=\"${esc(edge.anchor)}\">“${esc(truncate(edge.anchor))}” ↗</a>`\n    : (src ? ` <a class=\"cite\" href=\"${docHref(src.hash)}\">↗</a>` : '');\n  return `<li class=\"edge-row\">${arrow} ${methodBadge(edge.method, edge.confidence)}${cite}</li>`;\n};\n\nexport const spinner = '<p class=\"muted\">加载中…</p>';\nexport const errBox = (e) => `<p class=\"error\">出错了：${esc(e.message || e)}</p>`;\n\n// One full graph export per session, shared by home (axes/bridges) and graph view.\nlet graphCache = null;\nexport const fullGraph = async () => (graphCache ??= await api('graph'));\n\n// --- pure data shaping (kept DOM-free so a future React viewer reuses them) ---\n\nexport const areaOf = (path) => path.split('/')[0];\n\nexport const eraOf = (path) =>\n  path.startsWith('journal/imports/') ? 'roam'\n  : path.startsWith('journal/daily-feed/') ? 'feed'\n  : /^(knowledge|curriculum|learning-paths)\\//.test(path) ? 'knowledge'\n  : 'other';\n\nexport const ERA_LABELS = {\n  knowledge: '当前知识', roam: 'Roam 旧笔记 2021-23', feed: '每日 feed', other: '其他',\n};\n\n// {areas: [[name, count]…], eras: {era: count}, bridges: [{node, roam, knowledge}…]}\nexport const homeModel = (g) => {\n  const docs = g.nodes.filter((n) => n.kind === 'doc');\n  const byId = new Map(g.nodes.map((n) => [n.id, n]));\n  const areas = new Map();\n  const eras = {};\n  for (const d of docs) {\n    areas.set(areaOf(d.path), (areas.get(areaOf(d.path)) || 0) + 1);\n    eras[eraOf(d.path)] = (eras[eraOf(d.path)] || 0) + 1;\n  }\n  const hits = new Map(); // entity id → {roam, knowledge}\n  for (const e of g.edges) {\n    if (e.relation !== 'mentions') continue;\n    const a = byId.get(e.source);\n    const b = byId.get(e.target);\n    const doc = a?.kind === 'doc' ? a : b?.kind === 'doc' ? b : null;\n    const ent = a?.kind === 'entity' ? a : b?.kind === 'entity' ? b : null;\n    if (!doc || !ent) continue;\n    const era = eraOf(doc.path);\n    if (era !== 'roam' && era !== 'knowledge') continue;\n    const h = hits.get(ent.id) || { node: ent, roam: 0, knowledge: 0 };\n    h[era] += 1;\n    hits.set(ent.id, h);\n  }\n  const bridges = [...hits.values()]\n    .filter((h) => h.roam > 0 && h.knowledge > 0)\n    .sort((x, y) => Math.min(y.roam, y.knowledge) - Math.min(x.roam, x.knowledge)\n      || (y.roam + y.knowledge) - (x.roam + x.knowledge));\n  return { areas: [...areas.entries()].sort((x, y) => y[1] - x[1]), eras, bridges };\n};\n",
};
